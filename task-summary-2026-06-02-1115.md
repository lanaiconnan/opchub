# Task Summary 2026-06-02 11:15

## Objective
OPC MVP 项目 P0 改进：ChatPage 接入真实 LLM + ChatInput 组件优化

## Key Changes

### 1. 后端 `/opc/chat` 对话接口
- 路径：`POST /opc/chat`，参数 `{ opcId, messages }`
- 先尝试 Ollama LLM（qwen2.5:3b），不可用时自动 fallback 到增强版规则引擎
- 规则引擎覆盖：技能展示、报价建议、自我介绍优化、合作计划、项目信息查询
- System Prompt 注入 OPC 上下文（名称/描述/标签/预算/联系方式）

### 2. ChatPage.jsx 改造
- 移除前端规则匹配逻辑，改为调用后端 `/opc/chat`
- 发送完整对话历史（user+assistant），支持多轮上下文
- 错误提示：Ollama 不可用时显示友好提示

### 3. ChatInput.jsx 重写
- 单行 input → 多行 textarea（自动调高，max 120px）
- Enter 发送，Shift+Enter 换行
- 支持 disabled 状态（发送中禁用）
- 空输入禁用发送按钮

### 4. Home.jsx 新增按钮
- OPC 卡片右侧增加 "🤖 AI 协助" 按钮，跳转 `/chat/:id`

### 5. App.jsx 路由
- 新增 `/chat/:id` 路由指向 ChatPage

### 6. 后端修复（前置）
- `server.js` 中 `opcList` → `db.data.opcList` 变量引用错误已修复
- TF-IDF → Ollama Embedding（nomic-embed-text）已替换
- 卸载无用依赖 `natural`、`stopword`

## Technical Notes
- Ollama 模型拉取（qwen2.5:3b ~1.9GB、tinyllama ~637MB）因网速慢未完成
- 当前对话功能使用规则引擎 fallback，后续拉到模型后自动切换为 LLM
- 匹配接口 `GET /opc/match/:id` 已验证通过，返回 Embedding 相似度

## Files Modified
- `backend/server.js` — 修复 + Embedding + 对话接口
- `frontend/src/pages/ChatPage.jsx` — 新建，对话式协作界面
- `frontend/src/components/ChatInput.jsx` — 重写，多行输入
- `frontend/src/pages/Home.jsx` — 增加 AI 协助按钮 + 样式
- `frontend/src/App.jsx` — 增加 /chat/:id 路由
