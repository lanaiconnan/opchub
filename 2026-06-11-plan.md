# 2026-06-11 工作计划

## 今日已完成（2026-06-10）

- [x] ToastContext import bug 修复（default → named import）
- [x] 全局 "OPC" 文案替换为 "项目" / "Nexus"
- [x] RequireAuth.jsx token key 修复（`token` → `accessToken`）
- [x] 暗色主题全站迁移完成（所有页面已用 `useColors()`）
- [x] 构建验证通过（104 modules ✓）

---

## 🔴 高优先级 — 需要先确认需求

### 1. 分类统计（P2）
**状态：** 等待你确认需求
**问题：**
- 全局统计 or 个人统计？
- 展示位置：Home 页底部 / 侧边栏 / 独立页面？
- 统计维度：仅分类，还是加上技能、协作类型？

**下一步：** 你上线后告诉我需求，直接开始做。

---

## 🟡 中优先级 — 可以直接开始

### 2. Token 刷新机制（P1-1）
**状态：** 架构已设计，未实现
**上下文：**
- 当前只有 accessToken，无 refreshToken 逻辑
- `api.js` 里需要加 401 拦截 → 自动刷新 → 重试原请求
- 后端已有 `/auth/refresh` 端点（需确认）

**预计工作量：** 1-2 小时

### 3. 密码强度校验（P1-2）
**状态：** 前端校验 UI 已完成（Register.jsx 有密码强度条），后端未校验
**下一步：** 后端 `/auth/register` 加密码强度校验（最低8位+大小写+数字）

**预计工作量：** 30 分钟

---

## 🟢 低优先级 — 可以往后排

### 4. Docker 化（P2）
- 后端 Dockerfile + docker-compose.yml
- 前端 build 后用 nginx 镜像
- Ollama 容器化（可选）

### 5. 其他历史遗留
- [ ] `Settings.jsx` 暗色主题迁移（如果有这个页面）
- [ ] `MyApplications.jsx` 样式检查
- [ ] 前端 E2E 测试补全

---

## 🛠️ 环境备忘

| 项目 | 命令 |
|------|------|
| 前端 dev | `cd opc-mvp-workspace/frontend && npx vite` (port 5173) |
| 后端 dev | `cd opc-mvp-workspace/backend && node server.js` (port 3000) |
| 构建验证 | `cd frontend && npx vite build` |
| Ollama | `ollama serve`（确保 qwen2.5:0.5b 已拉取）|

**进程重启注意：**
- 不要用 `&` 后台启动，用 OpenClaw exec 的 `background=true`
- 或者直接用 `npm run dev`（如果配了）

---

## 📌 明天上线第一件事

1. 告诉我**分类统计的需求**（全局/个人、展示位置）
2. 或者直接让我开始做 **Token 刷新机制**
3. 我会直接上手，不再问优先级

---

_生成时间：2026-06-10 22:49 GMT+8_
